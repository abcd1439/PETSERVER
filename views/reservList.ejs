<html>
	<head>
	    <meta charset="UTF-8">
	    <link rel="shortcut icon" href="../images/ico_image.ico">
	    <title>Pettner</title>
	    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
		<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
	</head>
	<body>
		<nav class="navbar navbar-inverse">
			<div class="container-fluid">
	    		<div class="navbar-header">
	    			<a class="navbar-brand" href="#">Pettner</a>
	    		</div>
	    		<ul class="nav navbar-nav">
	    			<li><a href="/webHospital/<%= data[0].hospitalId %>">병원 소개</a></li>
	      			<li class="active"><a href="/webReserv/<%= data[0].hospitalId %>">예약 관리</a></li>
	    			<li><a href="/webUsers/<%= data[0].hospitalId %>">고객 관리</a></li>
	    			<li><a href="/webQna/<%= data[0].hospitalId %>">질문 관리</a></li>
	    		</ul>
	    		<ul class="nav navbar-nav navbar-right">
	      			<li><a href="/logout"><span class="glyphicon glyphicon-log-in"></span> Logout</a></li>
	    		</ul>
	  		</div>
		</nav>

		<div>	
			<ul>
		    	<% for(var i = 0 ; i < data.length ; i++ ) { %>
		        	
		        	예약시간<%=data[i].resDatetime%><br>
		        	<%if(data[i].status==1){%>
		        		대기중
		        		<form action="/webReserv/<%= data[0].hospitalId %>" method="post">
		        			<input type="hidden" name="id" id="id" value="<%=data[i].id%>" >
		        			<input type="hidden" name="status" id="status" value="2" >
		        			<button class="btn btn-primary" type="submit"><i class="glyphicon glyphicon-ok"></i></button>
		        		</form>
		        		<form action="/webReserv/<%= data[0].hospitalId %>" method="post">
		        			<input type="hidden" name="id" id="id" value="<%=data[i].id%>" >
		        			<input type="hidden" name="status" id="status" value="4" >
		        			<button class="btn btn-danger" type="submit"><i class="glyphicon glyphicon-remove"></i></button>
		        		</form>
		        	<%}%>
		        	<%if(data[i].status==2){%>
		        		예약완료<br>
		        	<%}%>
		        	<%if(data[i].status==3){%>
		        		진료완료<br>
		        	<%}%>
		        	<%if(data[i].status==4){%>
		        		예약취소<br>
		        	<%}%>
		        	
		        	예약과목<%=data[i].subject%><br>
		        	예약고객번호<%=data[i].userId%><br><br>
		      	<% } %>
		    </ul>
		</div>
	</body>
</html>